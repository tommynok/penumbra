Antumbra includes a TUI together with the CLI module.

The TUI can be enabled by compiling Antumbra with the `tui` feature flag.

![[antumbra-tui.png]]

As for now, the TUI is still WIP, and might have bugs.
It's suggested to use the [[CLI]] instead.
